function splitString(str) {
  let length = str.length;
  let splitlength = length / 2;
  console.log(splitlength);
  console.log(length);
  let result = str.split("");
  console.log(result);
}

splitString("abcd");
