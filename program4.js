let firstStr = "hello";

let secondStr = "world";
let concat = firstStr + secondStr;
console.log(concat);

let firstsub = firstStr.substring(1,firstStr.length);
let secondsub = secondStr.substring(1,secondStr.length);
console.log(firstsub + secondsub);